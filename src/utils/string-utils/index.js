// @flow
export {
  makeTitleString,
  normalizeStr,
  sentenceCase,
  upperAlphaChars,
  formatPercent,
  isQuotedString,
  extractQuotedString,
  unquoteString,
} from "./string-util";

export { valueToSuggestions, valuesToSuggestions } from "./suggest-utils";
